---

- name: Check System Disk Space
  hosts: all
   
  vars:
    minimum_diskspace: 10
      
  tasks:
    - name: Get disk facts
      win_disk_facts:
      
    - name: "Ensure that minimum free space on C: drive is 10GB"
      assert:
        that: size_remaining/1073741824 > minimum_diskspace
        msg: 'Remaining disk space has reached {{ size_remaining | filesizeformat }} need 10GB to proceed.'
      when: drive_letter == "C"
      loop: "{{ drive_letter }}"
      register: system_diskspace
