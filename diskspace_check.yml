---
- name: Check System Disk Space
  hosts: all
  vars:
    minimum_diskspace: 10
    win_disk_facts:
    
  tasks:        
    - name: "Ensure that minimum free space is 10GB"
      debug:
        msg: "{{ item.1 }}"
      loop: "{{ ansible_facts.ansible_disks | subelements('partitions') }}"
