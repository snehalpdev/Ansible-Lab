---
- name: Check System Disk Space
  hosts: all
  vars:
    minimum_diskspace: 10240
    win_disk_facts:
    register: disk_facts

  tasks:  
     - name: "Ensure that minimum free space is 10GB"
         msg: "{{ disk_facts.ansible_facts.disks[0].size_remaining }}"

      
